# Plugins

This directory is reserved for plugin examples, development resources, or bundled plugins in the future.

The plugin format and API have not been designed yet. Community plugin development should not begin until the core application architecture is stable.
